name: Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          python-version: "3.12"
          enable-cache: true
          cache-dependency-glob: |
            uv.lock
            examples/integrations/twilio/uv.lock
            examples/outbound_call/uv.lock
          prune-cache: true
      - run: uv sync --frozen
      - run: uv run pytest
      - run: uv sync --project examples/integrations/twilio --frozen
      - run: uv run --project examples/integrations/twilio pytest examples/integrations/twilio -q
      - run: uv sync --project examples/outbound_call --frozen
      - run: uv run --project examples/outbound_call --frozen pytest examples/outbound_call -q
